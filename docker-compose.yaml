services:
  db:
    image: postgres:17-alpine
    env_file:
      - .env
    ports:
      - "127.0.0.1:5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
